# API Keys (Required)
OPENAI_API_KEY=your_openai_api_key_here
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
HEYGEN_API_KEY=your_heygen_api_key_here

# AWS S3 Configuration (Required for file storage)
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=us-east-1
S3_BUCKET_NAME=digital-twin-storage

# Job Storage Configuration (Optional - defaults shown)
JOB_STORAGE=redis
REDIS_URL=redis://redis:6379
WORKERS=1

# File Settings (Optional - defaults shown)
MAX_FILE_SIZE_MB=50
TEMP_DIR=./temp
OUTPUT_DIR=./output

# Default Voice IDs (Optional - can be overridden per persona)
DEFAULT_ELEVENLABS_VOICE_ID=zqjPlH84bFLbo8q9PPo7
DEFAULT_HEYGEN_VOICE_ID=cb8c232f08a9466c870ad2c037fcf77a

# Default Avatar IDs (Optional - can be overridden per persona)
DEFAULT_HEYGEN_AVATAR_ID=129fa3d48fad41e4975c4e9471d953fb

# Notes:
# - For local development with Docker Compose, use the default Redis URL
# - For production deployment, set these as environment variables or secrets
# - S3 bucket must be created and configured with public read access
# - AWS credentials can be IAM roles instead of explicit keys for production
